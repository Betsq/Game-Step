@model Game_Step.Models.Publisher

@{
    ViewBag.Title = "Deletion " + Model.Name;
}

<div class="container">
    <form asp-action="Delete" asp-controller="Publisher" method="post">
        Name - @Model.Name

        <input type="submit" value="Delete" />
    </form>
</div>
